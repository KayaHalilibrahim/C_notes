#include<stdio.h>
#include<stdlib.h>

int main()
{


    // FILE *file;

   /*  char array[50][20];
    int i=0;
  if((file=fopen("mywork.txt","r"))!=0)
   {
       while(!feof(file))
    {
        fscanf(file,"%s",&array[i]);
        printf(" %s ",array[i]);
        i++;
    }
   }
   else
   {
       printf("the file could not find.");
   } */

fclose(file);

return 0;
}

// dosyada bulunan text i yazar.
// ornek halil ibrahim kaya 20.