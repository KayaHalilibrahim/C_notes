#include<stdio.h>
#include<stdlib.h>

int main()
{

char ch;
int sira;

printf("lutfen bir harf giriniz:");
scanf("%c",&ch);

if ((ch>='A')&&(ch<='Z'))
{
    sira=(int)ch-(int)'A'+1;
}
else if ((ch>='a')&&(ch<='z'))
{
    sira=(int)ch-(int)'a'+1;
}
else
{
    printf("wrongg!!");
}
printf("%d.harf",sira);

    return 0;
}