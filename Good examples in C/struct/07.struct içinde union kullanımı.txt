#include <stdio.h>
#include <stdlib.h>

struct degertip
{
    int tip;

    union
    {
    char kar;
    float sayi;

    }bir;
};



int main()
{

struct degertip degerler[20];
char satir;
int i=-1;
int j;
int n=0;
float ortalama=0.0;




do
{
    i++;

  printf("lutfen bir tam sayi giriniz:\n");
    scanf("%d",&degerler[i].tip);

  scanf("%c",&satir);  // hata vermemesi için..

    if(degerler[i].tip==1)
    {
        printf("karakter giriniz:\n");
        scanf("%c",&degerler[i].bir.kar);

            scanf("%c",&satir);

    }
    else if(degerler[i].tip==0)
    {
        printf("reel sayi giriniz:");
        scanf("%f",&degerler[i].bir.sayi);
    }

}while(degerler[i].tip==0 || degerler[i].tip==1);


for(j=0; j<=i; j++)
{
    if(degerler[j].tip==0)
    {
    ortalama+=degerler[j].bir.sayi;
    n++;
    }
}

    ortalama=ortalama/n;

    printf("reel sayilaarin ortalamasi: %f",ortalama);

    return 0;
}

********************

ooutput:

lutfen bir tam sayi giriniz:
0
reel sayi giriniz:10
lutfen bir tam sayi giriniz:
1
karakter giriniz:
k
lutfen bir tam sayi giriniz:
0
reel sayi giriniz:20
lutfen bir tam sayi giriniz:
5
reel sayilaarin ortalamasi: 15.000000

